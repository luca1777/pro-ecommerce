const InputcComponent = ({ onChange }) => {


    const handleOnchamge = () => {
    //     setInternal state
        // set puter state
    }

    return <div>
        <input onChange={handleOnchamge}></input>
    </div>
}